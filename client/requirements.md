## Packages
framer-motion | Complex animations for hero section and page transitions
lucide-react | Icons for the interface (already in base, but ensuring it's noted)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Discord (mocked redirect for now)
Dark mode default
